import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  textedFooter: {
    paddingBottom: "2rem",
    paddingTop: "2rem",
    textAlign: "center",
  },
}));

export default useStyles;
